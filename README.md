# My Python App

A Flask-based web application deployed on Render.

## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. Run locally: `flask run` or `gunicorn wsgi:app`

## Deployment
Configured for Render with `gunicorn wsgi:app`.
